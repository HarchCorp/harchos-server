# HarchOS Server v0.6.0 - Python Dependencies

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
websockets==13.1

# Database
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0

# Migrations
alembic==1.14.1

# Settings
pydantic==2.10.4
pydantic-settings==2.7.1

# Auth
PyJWT==2.10.1
email-validator==2.2.0

# HTTP client (for Electricity Maps API calls + backend proxy)
httpx==0.28.1

# Caching — Upstash Redis (REST API, no TCP needed)
upstash-redis==1.2.0
# Caching — Traditional Redis (async client with hiredis parser for performance)
redis[hiredis]==5.2.1

# Metrics
prometheus-client==0.21.1

# Compression
python-gzip==0.2.1;python_version<"3.12"

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx  # Also used for TestClient
